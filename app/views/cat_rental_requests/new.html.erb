
<form class="" action="<%= cat_rental_requests_url %>" method="post">

  <label for="cat">Cat</label>
  <select id="cat" type="text" name="cat_rental_request[cat_id]">
    <option disabled selected > -- Please Select --</option>
    <% Cat.all.each do |cat| %>
      <option value="<%= cat.id %>" > <%= cat.name %> </option>
    <% end %>
  </select>
  <br><br>

  <label for="start_date">Start Date</label>
  <input id="start_date" type="date" name="cat_rental_request[start_date]">

  <label for="end_date">End Date</label>
  <input id="end_date" type="date" name="cat_rental_request[end_date]">

  <input type="submit" value="submit request">

</form>
